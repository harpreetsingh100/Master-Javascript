// 1. Style
// It is used to give css to html elements. Use camelCase convention while using style.

// const body = document.body;
// body.style.backgroundColor = "red";

// 2.classList

// We can get all the classes of an element with classList. It has three methods:-
// 1)add('name')     => adds class
// 2)remove('name')  => removes class
// 3)toggle('name')  => toggles class
