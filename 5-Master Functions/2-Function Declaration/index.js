// When we store the returned value of a function in a variable it is called function declaration.

const addedNumber = function addTwoNumbers(num1, num2) {
  return num1 + num2;
};

addTwoNumbers(10, 10);
