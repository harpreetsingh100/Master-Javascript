let arrowFunction = (a, b) => {
  return a + b;
};

arrowFunction(5, 5);

// We can get rid of () if there is only one paramter
// We can get rid of {} and return if there is only one line of code
// Whenever we use curly braces we have to use return keyword every time.
