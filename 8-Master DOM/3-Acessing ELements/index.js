// 1. document.getElementById('name');
// 2. document.getElementsByClassName('name);
// 3. document.getElementsByTagName('name);
// 4. document.getElementsByClassName('name);
// 5. document.querySelector('#id');
// 6. document.querySelectorAll('#class');

// => Returns HTML Collection. It is similar to array. Only the length property works.You cannot use Array methods. But querySelector returns NodeList. Only length property works. We cannot use Array methods on Nodelist either.
