// 1.tagName => returns tag for element nodes
// 1.innerText => changes text content of element and the children
// 2.innerHTML => returns the plain text or HTML contents in the element
// 1.textContent => changes text content even for hidden elements

// We can manipulate it using = operator.If we want to keep the previous text and add the new one then we can use += operator.
